const versionNumber = 100;
const versionString = '1.0.0';

/*
 * version
 */
const version = (args) => {
  console.log(`One ${versionString}`);
};

module.exports = {
  versionNumber,
  versionString,
  version,
};
